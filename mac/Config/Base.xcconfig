// Repository defaults keep fresh checkouts buildable with ad-hoc signing.
CODE_SIGN_IDENTITY = -
DEVELOPMENT_TEAM =

// Optional local signing overrides are applied after the repository defaults.
#include? "Local.xcconfig"

// Version ownership is shared across both apps and cannot be shadowed by a
// local signing override.
#include "../../Config/Version.xcconfig"
