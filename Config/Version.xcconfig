// Shared customer-facing version defaults for both Apple apps.
// Keep the marketing version aligned with the App Store Connect records.
// GitHub TestFlight builds may override CURRENT_PROJECT_VERSION with the
// explicitly approved, never-reused build selected for that upload.
MARKETING_VERSION = 1.2
CURRENT_PROJECT_VERSION = 2
