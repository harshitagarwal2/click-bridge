Pair the phone before testing: on the connected Mac choose Pair Phone (or Replace Phone), then scan the QR code or open its shared HTTPS invitation. The link works when the phone is far away over the internet. Confirm that both devices show the same six-digit code and approve on the Mac. Without the native app, choose Copy PWA Invitation on the Mac for the /pair/web PWA fallback. Advanced Legacy relay and token fields are only for alternate or self-hosted deployments.

Please test all three click triggers while the iPhone app is in the foreground:

1. Change the system output volume once.
2. Tap Trigger 3 Clicks in the app.
3. Run the Trigger 3 Clicks App Shortcut.

For each trigger, confirm that the iPhone reports one action result with haptic feedback and that the connected Mac posts exactly three independent clicks at its current pointer location.

The Mac must be connected to the relay and must have Accessibility permission. Please also report which audio route was active (built-in speaker, wired/Bluetooth headset, or AirPods) when testing volume changes.
